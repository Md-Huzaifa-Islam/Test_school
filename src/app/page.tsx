import HomePage from "@/components/pages/HomePage";
import React, { FC } from "react";

/**
 * Root page component for the Test_School platform
 * Shows the home page with different content for authenticated/unauthenticated users
 */
const Home: FC = () => {
  return <HomePage />;
};

export default Home;
